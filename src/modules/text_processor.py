class TextProcessor:
    def process(self, text):
        """
        Processa o texto para preparação antes da indexação.

        :param text: O texto bruto a ser processado.
        :return: O texto processado.
        """
        # Implemente aqui a lógica de processamento de texto (tokenização, remoção de stop words, etc.)
        processed_text = text.lower()  # Exemplo simplificado de processamento
        return processed_text
class TextProcessor:
    def process(self, text):
        """
        Processa o texto para preparação antes da indexação.

        :param text: O texto bruto a ser processado.
        :return: O texto processado.
        """
        # Implemente aqui a lógica de processamento de texto (tokenização, remoção de stop words, etc.)
        processed_text = text.lower()  # Exemplo simplificado de processamento
        return processed_text
